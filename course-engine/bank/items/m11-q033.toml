# Bank item imported from practice/PRACTICE-EXAM.md — original educational content
#
# RETIRED 2026-08-14 — bd-tetz (near-duplicate adjudication, pair 23 of 24).
# Duplicate of m11-q102 (bank/items/m11-q102.toml). Measured by
# `cdcp_gate near-duplicate-items`: answer 100% · distractors 81% · stem 50%.
# Both were approved, so one mock form could draw both and assess the same
# proposition twice.
#
# WHY THIS COPY AND NOT m11-q102:
#   1. FORM — neither copy is drawn by the seed-42 published form, so the form does not
#      decide this pair; legs 2 and 3 do.
#   2. EXPLANATION — m11-q102 names the failure mode of NOT doing it ("congestion, risk, and
#      unmanageable MACs"); this copy restates its key.
#   3. PROVENANCE — imported from practice/PRACTICE-EXAM.md.
#
# Retired, never deleted: the bank is content-addressed and deleting the file would
# lose the provenance of the duplication. The rule that made this mechanical rather
# than case-by-case, and what stops the next import regrowing the class:
# bank/IMPORT-POLICY.md.
id = "mock40-q33"
kind = "single-select"
module = 11
stem = "Scalable network design in a DC emphasizes:"
choices = [
  "Random patching with no pathways",
  "Planned pathways, media, and topologies that support growth without chaos",
  "Copper only forever",
  "No labeling",
]
correct = "B"
explanation = "Scalable design plans pathways, media, and topologies so growth does not create chaos."
topic_ids = ["m11-planning", "m11-importance"]
objective_ids = []
bloom = "understand"
source_class = "original"
quantity_evidence = "qualitative_only"
status = "retired"
