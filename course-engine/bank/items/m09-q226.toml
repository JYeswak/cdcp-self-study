# Original CDCP educational MCQ — bank expansion (depth/apply)
#
# RETIRED 2026-08-14 — bd-tetz (near-duplicate adjudication, pair 18 of 24).
# Duplicate of m09-q156 (bank/items/m09-q156.toml). Measured by
# `cdcp_gate near-duplicate-items`: answer 66% · distractors 13% · stem 70%.
# Both were approved, so one mock form could draw both and assess the same
# proposition twice.
#
# WHY THIS COPY AND NOT m09-q156:
#   1. FORM — neither copy is drawn by the seed-42 published form, so the form does not
#      decide this pair; legs 2 and 3 do.
#   2. EXPLANATION — m09-q156 names both loops and the isolation function ("secondary coolant loops,
#      isolation, and heat exchange to the facility water or glycol plant"); this copy
#      compresses that to "the hydraulic and thermal interface". This pair is the
#      dangerous subclass: the keys are DIFFERENT letters (A vs B), so a reviewer
#      scanning answer keys would not see it.
#   3. PROVENANCE — q2xx "bank expansion (depth/apply)" wave.
#
# Retired, never deleted: the bank is content-addressed and deleting the file would
# lose the provenance of the duplication. The rule that made this mechanical rather
# than case-by-case, and what stops the next import regrowing the class:
# bank/IMPORT-POLICY.md.
id = "m09-q226"
kind = "single-select"
module = 9
stem = "In liquid-cooled designs, a CDU typically:"
choices = [
  "Routes copper only",
  "Controls and exchanges heat between IT coolant loops and facility water loops",
  "Is a type of ATS",
  "Measures PUE exclusively"
]
correct = "B"
explanation = "CDUs are the hydraulic and thermal interface for liquid IT cooling."
topic_ids = ["m09-liquid"]
objective_ids = []
bloom = "understand"
source_class = "original"
quantity_evidence = "qualitative_only"
status = "retired"
