# Bank item imported from practice/PRACTICE-EXAM.md — original educational content
#
# RETIRED 2026-08-14 — bd-tetz (near-duplicate adjudication, pair 4 of 24).
# Duplicate of bank-m14-q109 (bank/items/m14-q109.toml). Measured by
# `cdcp_gate near-duplicate-items`: answer 100% · distractors 63% · stem 75%.
# Both were approved, so one mock form could draw both and assess the same
# proposition twice.
#
# WHY THIS COPY AND NOT bank-m14-q109:
#   1. FORM — neither copy is drawn by the seed-42 published form, so the form does not
#      decide this pair; legs 2 and 3 do.
#   2. EXPLANATION — bank-m14-q109 explains why the alarm is worth installing ("buys response time
#      before water reaches critical gear"); this copy restates its own key.
#   3. PROVENANCE — imported from practice/PRACTICE-EXAM.md.
#
# Retired, never deleted: the bank is content-addressed and deleting the file would
# lose the provenance of the duplication. The rule that made this mechanical rather
# than case-by-case, and what stops the next import regrowing the class:
# bank/IMPORT-POLICY.md.
id = "mock40-q39"
kind = "single-select"
module = 14
stem = "Underfloor leak detection is installed primarily to:"
choices = [
  "Measure EMF",
  "Alarm on water near electrical/IT spaces before major damage",
  "Increase humidity always",
  "Unlock cages",
]
correct = "B"
explanation = "Underfloor leak detection alarms on water near electrical/IT spaces before major damage."
topic_ids = ["m14-leak"]
objective_ids = []
bloom = "remember"
source_class = "original"
quantity_evidence = "qualitative_only"
status = "retired"
