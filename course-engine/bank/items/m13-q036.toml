# Bank item imported from practice/PRACTICE-EXAM.md — original educational content
# Public syllabus heading: CDCP "Physical Security and Safety — Components for physical security" —
# https://www.epi-ap.com/services/1/3/4/Certified_Data_Centre_Professional_%28CDCP%29
# Official catalog/preview receipt: NFPA 731 (2026) —
# https://link.nfpa.org/all-publications/731/2026
# BLOCKED: retired duplicate; the public catalog does not expose the exact
# mantrap proposition needed to promote this item.
#
# RETIRED 2026-08-14 — bd-tetz (near-duplicate adjudication, pair 3 of 24).
# Duplicate of bank-m13-q077 (bank/items/m13-q077.toml). Measured by
# `cdcp_gate near-duplicate-items`: answer 64% · distractors 0% · stem 30%.
# Both were approved, so one mock form could draw both and assess the same
# proposition twice.
#
# WHY THIS COPY AND NOT bank-m13-q077:
#   1. FORM — neither copy is drawn by the seed-42 published form, so the form does not
#      decide this pair; legs 2 and 3 do.
#   2. EXPLANATION — bank-m13-q077 names the threat the interlock defeats ("reduce tailgating and
#      enable high-assurance checks"); this copy's explanation restates its own key
#      verbatim and teaches nothing. Two of this copy's distractors ("Cool people",
#      "Store copper spools") are answerable by spotting absurdity.
#   3. PROVENANCE — imported from practice/PRACTICE-EXAM.md.
#
# Retired, never deleted: the bank is content-addressed and deleting the file would
# lose the provenance of the duplication. The rule that made this mechanical rather
# than case-by-case, and what stops the next import regrowing the class:
# bank/IMPORT-POLICY.md.
id = "mock40-q36"
kind = "single-select"
module = 13
stem = "A mantrap is designed to:"
choices = [
  "Cool people",
  "Enforce controlled, interlocked entry so two doors are not open as a free path",
  "Store copper spools",
  "Bypass badge logs",
]
correct = "B"
explanation = "A mantrap enforces interlocked entry so two doors are not open as a free path."
topic_ids = ["m13-security-components"]
objective_ids = []
bloom = "remember"
source_class = "original"
quantity_evidence = "qualitative_only"
status = "retired"
