# Bank item imported from practice/PRACTICE-EXAM.md — original educational content
#
# RETIRED 2026-08-14 — bd-tetz (near-duplicate adjudication, pair 17 of 24).
# Duplicate of m09-q124 (bank/items/m09-q124.toml). Measured by
# `cdcp_gate near-duplicate-items`: answer 46% · distractors 63% · stem 63% [reshuffled-clone].
# Both were approved, so one mock form could draw both and assess the same
# proposition twice.
#
# WHY THIS COPY AND NOT m09-q124:
#   1. FORM — neither copy is drawn by the seed-42 published form, so the form does not
#      decide this pair; legs 2 and 3 do.
#   2. EXPLANATION — m09-q124 expands both acronyms (computer room air conditioner / computer room air
#      handler), which is the whole content of the distinction; this copy calls it
#      "interview shorthand" and expands neither.
#   3. PROVENANCE — imported from practice/PRACTICE-EXAM.md.
#
# Retired, never deleted: the bank is content-addressed and deleting the file would
# lose the provenance of the duplication. The rule that made this mechanical rather
# than case-by-case, and what stops the next import regrowing the class:
# bank/IMPORT-POLICY.md.
# Public syllabus heading: Types of cooling systems
# Official catalog/preview receipt: ISO/IEC 22237-4:2021 — https://www.iso.org/standard/78552.html
# BLOCKED: the current official receipt does not expose the exact CRAC-versus-CRAH distinction in this retired duplicate. No paid body, draft, or PDF was fetched.
id = "mock40-q26"
kind = "single-select"
module = 9
stem = "The crisp distinction between CRAC and CRAH is:"
choices = [
  "CRAC = typically DX/refrigerant-based room unit; CRAH = typically chilled-water air handler",
  "They are identical acronyms",
  "CRAH always includes a diesel",
  "CRAC only cools batteries",
]
correct = "A"
explanation = "Interview shorthand: CRAC is typically DX/refrigerant; CRAH is typically a chilled-water air handler."
topic_ids = ["m09-system-types"]
objective_ids = []
bloom = "remember"
source_class = "original"
quantity_evidence = "qualitative_only"
status = "retired"
