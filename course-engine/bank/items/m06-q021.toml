# Bank item imported from practice/PRACTICE-EXAM.md — original educational content
#
# RETIRED 2026-08-14 — bd-tetz (near-duplicate adjudication, pair 12 of 24).
# Duplicate of m06-q100 (bank/items/m06-q100.toml). Measured by
# `cdcp_gate near-duplicate-items`: answer 100% · distractors 100% · stem 57%.
# Both were approved, so one mock form could draw both and assess the same
# proposition twice.
#
# WHY THIS COPY AND NOT m06-q100:
#   1. FORM — neither copy is drawn by the seed-42 published form, so the form does not
#      decide this pair; legs 2 and 3 do.
#   2. EXPLANATION — m06-q100 adds the correction the item exists for ("It is not an availability
#      design") on top of the formula; this copy stops at the formula. The two carry
#      IDENTICAL distractor sets (100%) with the key moved D -> B.
#   3. PROVENANCE — imported from practice/PRACTICE-EXAM.md.
#
# Retired, never deleted: the bank is content-addressed and deleting the file would
# lose the provenance of the duplication. The rule that made this mechanical rather
# than case-by-case, and what stops the next import regrowing the class:
# bank/IMPORT-POLICY.md.
# Public syllabus heading: Power sustainability
# Official catalog/preview receipt: ISO/IEC 30134-2:2026 — https://www.iso.org/standard/30134-2?browse=ics
# PASS: the current ISO OBP page explicitly defines PUE as a data-centre energy-efficiency KPI. No standard body or PDF was fetched.
id = "mock40-q21"
kind = "single-select"
module = 6
stem = "PUE is defined as:"
choices = [
  "IT energy ÷ total facility energy",
  "Total facility energy ÷ IT equipment energy",
  "Water liters ÷ staff headcount",
  "Generator kW ÷ rack count",
]
correct = "B"
explanation = "PUE = total facility energy ÷ IT equipment energy; lower overhead is better."
topic_ids = ["m06-sustainability"]
objective_ids = []
bloom = "remember"
source_class = "original"
quantity_evidence = "qualitative_only"
status = "retired"
