# Original educational bank item — modules expansion (not mock40)
# Public syllabus heading: Monitoring challenges
# Official catalog/preview receipt: RFC 3411 — https://www.rfc-editor.org/rfc/rfc3411.html
# PASS: the open RFC explicitly describes SNMP management entities,
# notification applications, and the management protocol. No PDF was fetched.
id = "bank-m14-q122"
kind = "single-select"
module = 14
stem = "RFC 3411 describes an SNMP management system as containing:"
choices = [
  "Managed nodes with SNMP entities, management applications such as command/notification roles, and a management protocol",
  "Only a single cloud dashboard with no managed nodes",
  "Only a database schema, with no management applications or protocol",
  "No notification functions; SNMP is limited to file transfer"
]
correct = "A"
explanation = "RFC 3411 describes managed nodes with SNMP entities, SNMP entities with command and notification applications, and a management protocol conveying management information. The RFC does not describe BACnet/Modbus integration or a data-centre-specific multi-truth policy."
topic_ids = ["m14-monitoring-challenges"]
objective_ids = []
bloom = "understand"
source_class = "original"
quantity_evidence = "qualitative_only"
status = "approved"
