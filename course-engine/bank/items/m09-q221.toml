# Original CDCP educational item — bank expansion
# Public syllabus heading: Types of cooling systems
# Official catalog/preview receipt: ISO/IEC 22237-4:2021 — https://www.iso.org/standard/78552.html
# BLOCKED: this retired near-duplicate remains in the ledger, but the current environmental-control catalog does not expose the exact economization proposition.
#
# RETIRED 2026-08-14 — bd-tetz (near-duplicate adjudication, pair 16 of 24).
# Duplicate of m09-q108 (bank/items/m09-q108.toml). Measured by
# `cdcp_gate near-duplicate-items`: answer 80% · distractors 21% · stem 50%.
# Both were approved, so one mock form could draw both and assess the same
# proposition twice.
#
# WHY THIS COPY AND NOT m09-q108:
#   1. FORM — neither copy is drawn by the seed-42 published form, so the form does not
#      decide this pair; legs 2 and 3 do.
#   2. EXPLANATION — m09-q108 names the constraint that survives economization ("while still rejecting
#      IT heat") — the thing a learner gets wrong; this copy stops at "cut compressor
#      work".
#   3. PROVENANCE — q2xx "bank expansion (depth/apply)" wave.
#
# Retired, never deleted: the bank is content-addressed and deleting the file would
# lose the provenance of the duplication. The rule that made this mechanical rather
# than case-by-case, and what stops the next import regrowing the class:
# bank/IMPORT-POLICY.md.
id = "m09-q221"
kind = "single-select"
module = 9
stem = "Free cooling or economization aims to:"
choices = [
  "Use favorable outdoor conditions to reduce mechanical refrigeration energy",
  "Always eliminate all heat rejection equipment forever",
  "Heat the data hall intentionally in summer only",
  "Replace dual power paths"
]
correct = "A"
explanation = "Economizers leverage cool outdoor conditions to cut compressor work."
topic_ids = ["m09-system-types", "m09-principles"]
objective_ids = []
bloom = "understand"
source_class = "original"
quantity_evidence = "qualitative_only"
status = "retired"
