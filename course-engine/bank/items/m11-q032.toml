# Bank item imported from practice/PRACTICE-EXAM.md — original educational content
#
# RETIRED 2026-08-14 — bd-tetz (near-duplicate adjudication, pair 22 of 24).
# Duplicate of m11-q101 (bank/items/m11-q101.toml). Measured by
# `cdcp_gate near-duplicate-items`: answer 100% · distractors 88% · stem 88%.
# Both were approved, so one mock form could draw both and assess the same
# proposition twice.
#
# WHY THIS COPY AND NOT m11-q101:
#   1. FORM — neither copy is drawn by the seed-42 published form, so the form does not
#      decide this pair; legs 2 and 3 do.
#   2. EXPLANATION — m11-q101 states the contrast its distractors probe ("interconnection, not
#      mechanical plant storage"); this copy restates its key.
#   3. PROVENANCE — imported from practice/PRACTICE-EXAM.md.
#
# Retired, never deleted: the bank is content-addressed and deleting the file would
# lose the provenance of the duplication. The rule that made this mechanical rather
# than case-by-case, and what stops the next import regrowing the class:
# bank/IMPORT-POLICY.md.
id = "mock40-q32"
kind = "single-select"
module = 11
stem = "A meet-me room (MMR) is primarily for:"
choices = [
  "Battery watering",
  "Carrier and customer interconnection / cross-connects",
  "Diesel polishing",
  "CRAH filter storage only",
]
correct = "B"
explanation = "An MMR is the interconnection hub for carrier and customer cross-connects."
topic_ids = ["m11-planning", "m11-site-to-site"]
objective_ids = []
bloom = "remember"
source_class = "original"
quantity_evidence = "qualitative_only"
status = "retired"
