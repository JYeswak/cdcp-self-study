# Original CDCP educational MCQ — bank expansion (depth/apply)
#
# RETIRED 2026-08-14 — bd-tetz (near-duplicate adjudication, pair 24 of 24).
# Duplicate of m11-q139 (bank/items/m11-q139.toml). Measured by
# `cdcp_gate near-duplicate-items`: answer 100% · distractors 92% · stem 16%.
# Both were approved, so one mock form could draw both and assess the same
# proposition twice.
#
# WHY THIS COPY AND NOT m11-q139:
#   1. FORM — neither copy is drawn by the seed-42 published form, so the form does not
#      decide this pair; legs 2 and 3 do.
#   2. EXPLANATION — m11-q139 keeps both halves of the practice being tested ("Administration AND
#      documentation turn physical infrastructure into an operable system"); this copy
#      drops documentation and narrows the subject to "cables". m11-q139 is also
#      bloom = evaluate against this copy's apply. At 16% stem similarity this pair is the
#      one a stem-based detector could never have found.
#   3. PROVENANCE — q2xx "bank expansion (depth/apply)" wave.
#
# Retired, never deleted: the bank is content-addressed and deleting the file would
# lose the provenance of the duplication. The rule that made this mechanical rather
# than case-by-case, and what stops the next import regrowing the class:
# bank/IMPORT-POLICY.md.
# Public syllabus heading: Importance of network cabling infrastructure
# Official catalog/preview receipt: TIA-942-C, May 2024 — https://tiaonline.org/standard/tia-942/
# BLOCKED: the current TIA-942-C abstract does not expose the exact item-level importance of network cabling infrastructure proposition. No paid body or PDF was fetched.
id = "m11-q226"
kind = "single-select"
module = 11
stem = "For emergency isolation of a compromised circuit, the best enabler is:"
choices = [
  "Unlabeled bundles and tribal knowledge only",
  "Random recabling each shift",
  "Disabling monitoring",
  "Clear labeling, accurate records, and pre-planned separation of critical paths"
]
correct = "D"
explanation = "Administration turns cables into an operable system under stress."
topic_ids = ["m11-importance", "m11-planning"]
objective_ids = []
bloom = "apply"
source_class = "original"
quantity_evidence = "qualitative_only"
status = "retired"
