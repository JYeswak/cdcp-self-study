# Original CDCP educational MCQ — bank expansion (not an exam dump)
# Public syllabus heading: CDCP "Causes of unavailability" —
# https://www.epi-ap.com/services/1/3/4/Certified_Data_Centre_Professional_%28CDCP%29
# Official catalog/preview receipt: ISO/IEC 22237-1:2021 —
# https://www.iso.org/standard/78550.html?browse=tc
# BLOCKED: the catalog addresses availability classification, but not this
# exact single-point-of-failure proposition.
id = "m01-q206"
kind = "single-select"
module = 1
stem = "A single point of failure in facility design is best characterized as:"
choices = [
  "Any sensor that reports humidity, because a measurement point is itself a single point of failure",
  "A component or path whose failure alone can interrupt the critical load",
  "Any dual-path system by definition, even when one path cannot carry the critical load during maintenance",
  "Any redundant UPS string is typically a single point of failure, regardless of whether another independent path reaches the critical load"
]
correct = "B"
explanation = "The single point of failure is a component or path whose lone failure can interrupt the protected load; a sensor, a dual-path system with inadequate capacity, or a redundant UPS string is not automatically such a point."
topic_ids = ["m01-unavailability", "m01-elements"]
objective_ids = []
bloom = "apply"
source_class = "original"
quantity_evidence = "qualitative_only"
status = "approved"
