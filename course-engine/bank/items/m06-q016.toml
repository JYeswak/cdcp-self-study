# Bank item imported from practice/PRACTICE-EXAM.md — original educational content
#
# RETIRED 2026-08-14 — bd-tetz (near-duplicate adjudication, pair 8 of 24).
# Duplicate of m06-q053 (bank/items/m06-q053.toml). Measured by
# `cdcp_gate near-duplicate-items`: answer 91% · distractors 63% · stem 44%.
# Both were approved, so one mock form could draw both and assess the same
# proposition twice.
#
# WHY THIS COPY AND NOT m06-q053:
#   1. FORM — neither copy is drawn by the seed-42 published form, so the form does not
#      decide this pair; legs 2 and 3 do.
#   2. EXPLANATION — m06-q053 gives the definition in the sizing language the exam uses ("dual
#      independent systems/paths, each sized for N"); this copy restates its own key.
#   3. PROVENANCE — imported from practice/PRACTICE-EXAM.md.
#
# Retired, never deleted: the bank is content-addressed and deleting the file would
# lose the provenance of the duplication. The rule that made this mechanical rather
# than case-by-case, and what stops the next import regrowing the class:
# bank/IMPORT-POLICY.md.
id = "mock40-q16"
kind = "single-select"
module = 6
stem = "2N power architecture conceptually provides:"
choices = [
  "A single path with oversized cables",
  "Two complete independent capacity paths each able to carry the load",
  "N capacity with no spares",
  "Only generator redundancy without UPS",
]
correct = "B"
explanation = "2N provides two complete independent capacity paths, each able to carry the full load."
topic_ids = ["m06-redundancy"]
objective_ids = []
bloom = "remember"
source_class = "original"
quantity_evidence = "qualitative_only"
status = "retired"
