# Original CDCP educational MCQ — bank expansion (depth/apply)
#
# RETIRED 2026-08-14 — bd-tetz (near-duplicate adjudication, pair 2 of 24).
# Duplicate of bank-m12-q075 (bank/items/m12-q075.toml). Measured by
# `cdcp_gate near-duplicate-items`: answer 92% · distractors 93% · stem 76%.
# Both were approved, so one mock form could draw both and assess the same
# proposition twice.
#
# WHY THIS COPY AND NOT bank-m12-q075:
#   1. FORM — neither copy is drawn by the seed-42 published form, so the form does not
#      decide this pair; legs 2 and 3 do.
#   2. EXPLANATION — bank-m12-q075's key and explanation both name the misuse being excluded
#      ("not a casual disable" / "not unauthorized system disablement"); this copy's
#      "not system abandonment" is vaguer about what is actually prohibited.
#   3. PROVENANCE — q2xx "bank expansion (depth/apply)" wave.
#
# Retired, never deleted: the bank is content-addressed and deleting the file would
# lose the provenance of the duplication. The rule that made this mechanical rather
# than case-by-case, and what stops the next import regrowing the class:
# bank/IMPORT-POLICY.md.
id = "m12-q217"
module = 12
stem = "Abort switches on gaseous systems, where provided, are intended to:"
choices = [
  "Mute the fire alarm forever",
  "Bypass AHJ inspections",
  "Provide a supervised means to delay discharge during investigation—not a casual permanent disable",
  "Open all wet-pipe heads immediately"
]
correct = "C"
explanation = "Abort is a controlled investigation tool under procedure, not system abandonment."
topic_ids = ["m12-gas-suppression", "m12-signage", "m12-suppression-reqs"]
objective_ids = []
bloom = "understand"
source_class = "original"
quantity_evidence = "qualitative_only"
status = "retired"
