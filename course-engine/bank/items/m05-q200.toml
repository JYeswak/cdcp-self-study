# Original CDCP educational MCQ — bank expansion (not an exam dump)
#
# RETIRED 2026-08-14 — bd-tetz (near-duplicate adjudication, pair 6 of 24).
# Duplicate of m05-q135 (bank/items/m05-q135.toml). Measured by
# `cdcp_gate near-duplicate-items`: answer 61% · distractors 20% · stem 60%.
# Both were approved, so one mock form could draw both and assess the same
# proposition twice.
#
# WHY THIS COPY AND NOT m05-q135:
#   1. FORM — neither copy is drawn by the seed-42 published form, so the form does not
#      decide this pair; legs 2 and 3 do.
#   2. EXPLANATION — m05-q135's explanation gives the physical definition ("luminous flux density on
#      a surface — the usual work-plane light level metric"); this copy's "light level on
#      a surface" restates the key in plainer words.
#   3. PROVENANCE — q2xx "bank expansion" wave.
#
# Retired, never deleted: the bank is content-addressed and deleting the file would
# lose the provenance of the duplication. The rule that made this mechanical rather
# than case-by-case, and what stops the next import regrowing the class:
# bank/IMPORT-POLICY.md.
id = "m05-q200"
kind = "single-select"
module = 5
stem = "Illuminance (lux or foot-candles) measures:"
choices = [
  "The luminous flux incident on a surface per unit area",
  "Only the color temperature of sunlight outdoors",
  "Only electrical watts at the panel",
  "Sound pressure in the white space"
]
correct = "A"
explanation = "Illuminance is light level on a surface—key for task visibility in technical spaces."
topic_ids = ["m05-measurements"]
objective_ids = []
bloom = "remember"
source_class = "original"
quantity_evidence = "qualitative_only"
status = "retired"
