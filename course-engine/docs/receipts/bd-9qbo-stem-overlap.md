# bd-9qbo stem-overlap cue

## Exposure

The stale bead headline was 425 applicable items at 38.6% keyed overlap.
Re-measuring after the hedge work with the harness's exact content-word and
stop-word definition found 440 bank items, 171 keyed unique maxima (38.9%),
and a whole-bank rate of 18.4% (171/931). Through the real assembly path over
the predeclared seeds 0..99, the product denominator was 1,897 applicable
instances with 615 keyed maxima (32.4%). Both populations confirmed the cue,
but neither justified rewriting all candidates.

## Targeted repair

57 approved items received content-safe distractor rewrites that pull stem
vocabulary into a clearly false competing proposition. No `stem` or `correct`
field changed. The remaining bank-level applicable population is 416 items,
with 123 keyed maxima (29.6%).

## Product result

The same assembly seeds 0..99 now report:

| strategy | mean / 40 | min | max | pass >=27 |
|---|---:|---:|---:|---:|
| stem-overlap | 11.15 | 5 | 18 | 0/100 |
| uniform-random | 9.94 | 5 | 20 | 0/100 |

The applicable-only product rate is 499/1,835 = 27.2%, inside the 20--30%
asserted band. The harness now asserts both that band and the 9--11.5/40
stem-overlap control band. The 9--11.5 range is deliberately stated as a
measurement band around the 9.94 random control, not as a claim of item
quality.

## Should-fail

`m09-q119` was left unchanged. Pulling “localized inlet hot spot” into the
distractor “A deliberately heated cold aisle caused by airflow failure” would
make that distractor an arguably true competing description rather than a
false option. This item is a review residual, not silently counted as repaired.

## Verification

`construction-faults` live verdict PASS with zero absolute-language
distractors and length-rank-uniformity PASS at 27.3/24.8/23.6/24.3%.
`answer-key-skew`, `verify-bank`, `key-contradiction`, `verify-orphans`,
`validate-grounding`, and `near-duplicate-items` pass. Assembly tests are 9/9;
formatting and clippy are clean.

This closes the measured stem-overlap guessing route for the product sample;
it does not establish truth, discrimination, or certification.
