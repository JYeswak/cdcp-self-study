# UX findings log — learner surface

**Consumer:** the de-duplication pass, then the fix beads it produces.
**Gate:** operator will not move on until 100–500 observed runs are logged and de-duplicated.
**Deletion condition:** retires when every surviving finding is either a closed bead or an
explicitly declined entry with a reason. This file is not a permanent artifact.

**Scope note.** `ui-ux-pro-max` is written for app UI (iOS/Android/React Native). This is a
local-first static HTML + WASM surface. Contrast, spacing rhythm, hit-target size, motion
timing, focus order and a11y transfer directly. Safe-area insets, bottom-nav limits, Phosphor/RN
component guidance and Dynamic Type do **not** — do not log findings that only exist because a
phone rule was applied to a desktop page.

## Entry format — one line per observation, append only

```
| id | surface | viewport | mode | severity | finding | why it matters | suggested change |
```

- **id** — `<pane>-<nnn>`, e.g. `p2-014`. Unique, never reused.
- **surface** — `hub` `mock` `quiz` `drill` `results` `learn` `learn/<module>` `reference` `runbooks` `diagram`
- **viewport** — `375` `768` `1280` `1920`, or `n/a`
- **mode** — `mouse` `keyboard` `dark` `light` `reduced-motion` `closed-notes` `first-run` `returning` `post-fail`
- **severity** — `blocker` (cannot complete the task) · `major` (completes but wrong/painful) ·
  `minor` (polish) · `question` (needs a human decision, not a defect)
- **finding** — what you observed, concretely. Not "improve the header".
- **why it matters** — the learner consequence. If you cannot name one, do not log it.
- **suggested change** — one specific change. "Make it nicer" is not a suggestion.

## Rules

1. **Observe, do not speculate.** Log what the page did, at a URL, at a viewport. If you did not
   load it, do not log it.
2. **One finding per line.** A line containing "and also" is two findings.
3. **No duplicates within your own pane** — check your existing ids before appending.
4. Cross-pane duplicates are expected and get merged in the de-dup pass. Do not coordinate to
   avoid them; independent rediscovery is signal about severity.
5. **Content defects count.** If an item reads badly, is ambiguous, or has an obviously-wrong
   option, log it — the exam experience includes the questions.
6. **Log what is GOOD too**, marked `severity=keep`. A redesign that discards a working thing is
   a regression, and we will not remember what worked.

## Seeded findings (controller, 2026-08-21)

| id | surface | viewport | mode | severity | finding | why it matters | suggested change |
|---|---|---|---|---|---|---|---|
| c-001 | hub | 1280 | first-run | minor | `GET /favicon.ico` 404s on every page load; no favicon is set | The browser tab is how a learner finds this among a dozen tabs during a study session; a blank tab icon reads as unfinished | Ship a favicon; it is one file and one link tag |
| c-002 | hub | 1280 | first-run | major | The h1 lead paragraph is operating instructions: "Start with cdcp serve and open http://127.0.0.1:8766/. Double-clicking this file (file://) is not supported and fails as CDCP_FILE_ORIGIN" | The learner is already served and already here — they are reading instructions for reaching a place they have reached. `CDCP_FILE_ORIGIN` is an internal error code shown to a learner | Move the file:// guidance to the error state that actually fires it; lead with what to do next |
| c-003 | mock | 1280 | first-run | major | Pre-exam instruction paragraph carries six distinct facts in one block: key bindings, timer semantics, submit behaviour, tab persistence, seed identity, closed-notes | Highest cognitive load lands immediately before a 60-minute timed task; a first-timer cannot absorb it and a returning learner does not need it | Split: keys into a dismissible shortcut hint, timer/submit semantics into the timer tooltip, seed into the seed control |
| c-004 | mock | 1280 | first-run | minor | Header line is `40Q · 60:00 · study bar 27/40 · study signal / not a pass mark` — four facts middot-separated | Dense middot lists do not scan; the one number that matters during the exam (time left) has equal weight to a disclaimer | Give the timer its own visual weight; demote the disclaimer to the footer where it already appears |
| c-005 | mock | 1280 | first-run | major | The Seed control is a `<select>` containing exactly one option, "42" | A control that looks interactive and offers no choice is a dead end; a learner who wants a different exam finds a dropdown that cannot do it | Either populate real seed choices or render it as static text, not a combobox |
| c-006 | mock | 1280 | first-run | minor | Item header exposes the internal id: "Item 1 of 40 · m01-q201" | Developer data in the learner surface; it means nothing to a candidate | Keep the id but demote it into a "report this question" affordance, which also gives us a defect channel |
| c-007 | mock | 1280 | first-run | major | Option lengths within one item are wildly uneven — A is 5 words, C is 15 ("Lightning strike with no process involvement, no equipment configuration error, and no organizational contribution") | Uneven options do not scan as a set, and an over-qualified distractor reads as wrong on sight regardless of knowledge | Content fix, not CSS: this is the construction-fault work reaching the surface. Log the item ids |
| c-008 | hub | 1280 | first-run | question | "Drill" appears as three separate cards — Session · Drill-10, Practice · Drill, Session · Miss-review — plus a Drill nav item | A learner cannot tell which drill they want without reading three descriptions | Decide whether these are one surface with modes or three surfaces, and name them accordingly |
| c-009 | hub | 1280 | first-run | question | Card copy says "14 curriculum modules + ops expansions"; the corpus has 15 module files | Either the count is stale or m15 is deliberately excluded as an "ops expansion" — a learner counting modules will find the mismatch | Verify and state the real number |
| c-010 | hub | 1280 | first-run | keep | "Skip to main content" link is present and first in tab order | This is correct a11y that a redesign would plausibly discard | Keep it; verify it survives any header rework |
| c-011 | hub | 1280 | first-run | keep | Empty state for "Now · 90 seconds" reads "No cards due. Take a mock or quiz, then come back for a 90-second loop." | Names the next action instead of showing a blank panel — a genuinely good empty state | Keep this pattern and reuse it for other empty surfaces |
| p3-001 | learn/06 | 375 | keyboard | major | The generated "On this page" TOC clips the long entry "Interconnect queue and behind-the-meter as a primary path" at the right edge. | A learner cannot read the full section name on the narrowest supported device. | Constrain the mobile TOC and make long entries wrap or scroll with an obvious affordance. |
| p3-002 | learn/07 | 375 | keyboard | major | The generated "On this page" TOC clips the long "Further free resources (public standards names, vendor ...)" entry at the right edge. | A learner cannot read the full destination before activating the link. | Make every mobile TOC label fully reachable and verify long labels at 375px. |
| p3-003 | learn/06 | 375 | keyboard | major | The visible shell h2 "Learning objectives" precedes the article h1 "Power Infrastructure" in the DOM heading order. | Screen-reader heading navigation reaches a subsection before the module identity. | Render the module h1 before the shell h2 or make the shell title a non-heading status element. |
| p3-004 | learn/07 | 375 | keyboard | major | The visible shell h2 "Learning objectives" precedes the article h1 "Electro Magnetic Fields (EMF)" in the DOM heading order. | Screen-reader heading navigation reaches a subsection before the module identity. | Render the module h1 before the shell h2 or make the shell title a non-heading status element. |
| p3-005 | learn/11 | 375 | keyboard | major | The visible shell h2 "Learning objectives" precedes the article h1 "Scalable Network and Cabling Infrastructure" in the DOM heading order. | Screen-reader heading navigation reaches a subsection before the module identity. | Render the module h1 before the shell h2 or make the shell title a non-heading status element. |
| p3-006 | reference | 375 | keyboard | major | The rendered "How to use" link points to `../practice/DRILL-CARDS.md`, which returns HTTP 404 from the running learner server. | A learner following the study instruction reaches a dead end before practice. | Replace it with a served learner URL or remove the link and check rendered reference links. |
| p3-007 | reference | 375 | keyboard | major | The glossary table extends beyond the 375px reading width; its Definition column is off-screen without a visible horizontal-scroll cue. | Key explanations require panning and a learner may miss the definition column. | Stack term and definition on mobile or provide a labelled scroll affordance. |
| p3-008 | diagrams/power-path | 375 | keyboard | major | The interactive power-path nodes render as `?` buttons with no aria-label or title; their accessible name is only "?". | A keyboard or screen-reader learner cannot identify a focused node before activating it. | Give each node an accessible name derived from its target, such as "Reveal Utility feed node". |
| p3-009 | runbooks | 375 | keyboard | major | The cross-page link rail under the introduction runs past the narrow viewport, ending visibly at `... Module quiz · M...`. | The learner may not be able to reach the later destinations on mobile. | Convert the rail to a wrapping link list with each destination label fully visible. |
| p3-010 | runbooks | 375 | keyboard | major | The long-form vignette introduction and first stem are clipped at the right edge instead of showing the complete sentence in the reading column. | Scenario context can be missing before the learner chooses where to practice. | Enforce text containment and overflow wrapping on vignette content and verify at 375px. |
| p3-011 | reference | 375 | content | major | The rendered glossary says "this program's 14 modules" while the learner hub exposes 15 module pages from 01 through 15. | The reference teaches a stale corpus count and undermines trust in the study map. | Derive the count from the module index or remove the hard-coded number. |
| p3-012 | cross-cutting | 375/768/1280/1920 | dark | question | The learner surface declares `color-scheme: dark` and has no light-theme override. | Learners who need a light palette cannot choose one, and the light-mode contrast case cannot be established. | Decide whether dark-only is intentional; otherwise add a tested light theme and contrast contract. |
| p3-013 | cross-cutting | 375/768/1280/1920 | reduced-motion | keep | The reduced-motion rule collapses animation and transition durations, disables smooth scrolling, and removes skip-link transition. | Motion-sensitive and keyboard learners are not forced through the default animated transitions. | Preserve this rule and add a regression check when new motion is introduced. |
| p3-014 | cross-cutting | 375/768/1280/1920 | dark | keep | The observed dark-theme token ratios against `#060b09` are 13.35:1 body, 7.73:1 secondary, 5.18:1 faint-caption, 11.87:1 amber, and 10.31:1 accent. | The shipped dark palette clears WCAG AA contrast for the observed text hierarchy. | Keep these ratios when palette tokens change. |
| p3-015 | diagrams/power-path | 375/1280 | keyboard | keep | The power-path self-check radios use fieldsets with legends and each radio is wrapped in a text-bearing label. | A keyboard learner can identify each question and option without relying on layout. | Preserve the fieldset, legend, and label structure. |
| p3-016 | learn | 1280/1920 | dark | keep | At 1280 and 1920 the long-form modules keep a separate TOC and a constrained prose column instead of expanding across the full viewport. | Reading line length stays usable on wide monitors. | Preserve the max-width and two-column reading layout. |
| p2-001 | mock | 375 | first-run | minor | At 375px the 40-question flow is 1917px tall; the submit button sits below the answer card and the five-row jump grid at y=1704. | A learner working on a narrow screen must scroll past the entire navigation strip to finish the task. | Keep a compact submit action visible after answering or provide a sticky exam action bar. |
| p2-002 | mock | 375 | first-run | keep | The answer cards, five-row 1–40 jump grid, and submit control fit within the 375px layout with no horizontal overflow. | A narrow-screen learner can reach every control without sideways panning. | Preserve the contained card and chip layout when the exam CSS changes. |
| p2-003 | mock | 768 | keyboard | keep | All advertised answer keys A–D and 1–4 selected the corresponding choice, while ArrowLeft/ArrowRight and P/N moved between questions in a fresh mock. | A keyboard-only learner can complete the core answer and navigation loop without a pointer. | Preserve the shortcut mapping and keep it covered by a browser-level test. |
| p2-004 | mock | 768 | closed-notes | keep | Enabling Closed notes changed Learn, Drill, and Reference to `aria-disabled=true` and `tabindex=-1`; activating Drill left the URL unchanged and announced the lock, while unchecking restored the links. | The timed-exam mode prevents accidental study-surface exits without silently trapping the learner. | Preserve the explicit status message and the unlock behavior. |
| p2-005 | mock | 768 | returning | keep | After answering question 1, moving to question 2, and reloading, the mock returned to `2 / 40` and retained `1 of 40 answered`. | A reload does not lose a learner's position or draft progress. | Keep session draft persistence and its pack-identity check. |
| p2-006 | mock | 768 | returning | major | After submitting a 40/40 mock, Back to mock reopened question 40 with all 40 answers still present and Submit attempt enabled; there was no new-attempt control. | A returning learner has no obvious way to start a fresh mock and can accidentally resubmit the completed draft. | Clear or archive the draft after grading and expose an explicit Start new attempt action. |
| p2-007 | mock | 768 | first-run | keep | Submit remained disabled at both 0 and 1 answered, with the hint stating the exact count; after all 40 choices it became enabled and the hint changed to ready to submit. | The completion rule is visible before a learner reaches the end of the exam. | Preserve the count-based gate and its ready-state copy. |
| p2-008 | mock | 768 | timer-expired | keep | With all 40 answers present and the timer forced past its deadline, the page showed `00:00`, `Timer expired at 00:00 — you may still submit`, kept Submit enabled, and graded the attempt at 40/40. | Expiry does not falsely discard a completed attempt or contradict the advertised soft-timer behavior. | Preserve the expired-state label and the ability to submit. |
| p2-009 | quiz | 375 | first-run | keep | The module picker exposed 15 module options and starting Module 01 clearly changed the progress to `1 / 12` for its bounded quiz sample. | A learner can see both the available module scope and the size of the selected quiz. | Preserve the explicit module picker and per-quiz progress count. |
| p2-010 | quiz | 375 | first-run | major | Item `m01-q046` asks what an edge or micro data centre is for, but A `Serve only as a diesel storage yard`, B `Replace all hyperscale cloud regions globally`, and D `Eliminate the need for any cooling` are cartoon distractors beside the keyed latency answer C. | A learner can eliminate three choices without knowing the concept, weakening the quiz as a knowledge check. | Replace the three cartoon distractors with plausible wrong applications of edge computing. |
| p2-011 | drill | 768 | first-run | keep | An empty default Drill reports `missed 0 · due 0`, says `No cards due — take a mock or quiz`, and keeps the missed, due, and all-card sections visibly distinct; the due and miss URLs narrow to their requested section. | Empty review does not look broken and each entry point has a predictable scope. | Preserve these action-oriented empty states and mode-specific sections. |
| p2-012 | drill | 768 | post-fail | keep | After a 3/12 module quiz, Drill reported 9 missed items, rendered nine miss-review cards, and showed the same nine cards in its all-card SRS table with due time stated as about 24 hours. | A failed quiz produces a concrete, countable recovery queue instead of losing the learner's misses. | Preserve the miss count, item ids, explanations, and due metadata. |
| p2-013 | results | 375 | post-fail | major | An all-wrong mock produced a 15,233px results page; the Open Drill link was at y=15,152 after a 13,515px item-review section. | The recovery action is effectively buried beneath 40 reviews at the moment a tired learner needs it most. | Put Open Drill and a concise recovery summary before the per-item review, with a second link at the end. |
| p2-014 | results | 768 | post-fail | keep | The 0/40 result showed the study-signal threshold, all 15 weak modules, each chosen/correct pair with an explanation, and a digest from the WASM grader. | A learner can see both the diagnosis and the auditable grading record instead of receiving only a score. | Preserve the separation between study signal, weak-module review, and GradeExact digest. |
| p2-015 | results | 768 | first-run | keep | Opening results without an attempt showed `No attempt found in sessionStorage. Take the mock exam first, then submit.` and provided Back to mock and Hub links. | A direct or stale results URL gives a next action instead of an empty or misleading score screen. | Preserve this fail-closed no-attempt state and its recovery links. |
| p2-016 | quiz | 375 | keyboard | minor | The quiz accepted all eight A–D and 1–4 answer shortcuts in testing, but its visible question screen did not tell the learner that those shortcuts exist. | Keyboard users can benefit from the feature only if they discover it; the mock advertises the same controls while quiz does not. | Add the same concise keyboard hint to the quiz exam state. |
| p2-017 | results | 768 | post-pass | keep | An all-correct mock rendered `40 / 40`, a WASM digest, the practice-bar comparison, and `None flagged` for weak modules without claiming certification. | A strong result is encouraging while retaining the boundary between study feedback and credentialing. | Preserve the explicit non-credential language and the no-weak-modules state. |
| p2-018 | mock | 375 | keyboard | major | With all 40 answers complete, Tab order sends a keyboard learner through Next and all 40 jump buttons before reaching Submit attempt at tab stop 42. | The advertised hotkeys answer and navigate, but keyboard-only submission still requires 40 redundant focus stops. | Move Submit before the jump strip in tab order or add a skip-to-submit link. |
| p3-017 | learn/01–15 | 375/1280 | keyboard | major | The generated Learn shell puts a visible `Learning objectives` h2 before the article h1 on all 15 module pages; this was reproduced at both widths, not only on 06, 07, and 11. | Heading navigation presents a subsection before the module identity on every long-form lesson, so the accessibility defect is systemic rather than an isolated module typo. | Render the article h1 before shell subsections, or make the shell label a non-heading element, then assert one coherent outline per module. |
| p3-018 | learn/01–15 | 375/1280 | keyboard | keep | All 15 module URLs returned HTTP 200 at both viewports; every generated TOC link resolved to an existing in-page id, document width stayed at the viewport width, and the prose column measured about 335px at 375 and 672px at 1280. | The long-form surface has a working page/TOC contract and constrained reading width across the complete module set, even though individual long labels still need the mobile clipping fix already logged. | Preserve the generated TOC ids, max-width reading column, and no-horizontal-overflow behavior while repairing label presentation. |
| p3-019 | reference | 375/1280 | keyboard | major | The page exposes two top-level h1 elements in order: `Reference`, then `CDCP Self-Study Glossary`; the glossary is therefore a second document root in the same heading outline. | Screen-reader heading navigation cannot tell whether the glossary is a peer page or content under the Reference page identity. | Keep one page h1 and demote the glossary title to h2 (or remove the redundant shell title), then test the outline at both widths. |
| p3-020 | diagrams/* (8 pages) | 375/1280 | keyboard | keep | All eight diagram URLs returned HTTP 200 at both widths; each started with one h1, its in-page hash link resolved, and neither body nor document exceeded the viewport width. | The interactive diagram catalogue has a consistent load, heading, anchor, and containment baseline across the complete set. | Preserve this baseline when adding scenarios or changing diagram controls. |
| p3-021 | m06 power / bank items | content | study-then-mock | keep | After reading the m06 path, redundancy, BESS, and sizing sections, the sampled items `m06-q013`, `m06-q052`, `m06-q089`, and `m06-q225` tested concepts the lesson explicitly teaches: utility-to-rack sequence, N+1 at the module layer, dual-purpose BESS controls, and kW = kVA × PF. | A learner can reach the keyed answer from the supplied m06 lesson without relying on an unstated term or a different definition. | Preserve the lesson/item terminology and keep the module-to-item review in the content pipeline. |
| p3-022 | m07 EMF / bank items | content | study-then-mock | keep | The m07 lesson explicitly covers power-frequency quasi-static magnetic fields, aluminium’s weakness for 50/60 Hz B shielding, apertures and seams, and specialist HEMP scope; those claims line up with `m07-q041`, `m07-q046`, `m07-q053`, and `m07-q054`. | The applied distinctions tested by the sample are present in the reading, including the important “do not claim commercial HEMP hardening” boundary. | Preserve the frequency, field-type, and specialist-scope terminology across lesson edits and item review. |
| p3-023 | m10 water / bank items | content | study-then-mock | keep | The m10 lesson teaches separate process/domestic/fire water, closed-loop interfaces and permit limits, and site-specific WUE boundaries; these align with `m10-q207`, `m10-q216`, and `m10-q217`. | The learner is not asked to infer that “closed loop” means zero water or automatic permitting; the lesson teaches the same bounded rejection. | Preserve the explicit fire/domestic distinction and the no-universal-permit language. |
| p3-024 | m10 water / m10-q300 | content | study-then-mock | major | `m10-q300` requires the applied leak sequence “contain and isolate, protect nearby electrical/IT assets, notify, and document,” but the m10 lesson only states that closed loops need leak response and that detection/isolation matter; it does not teach the full response sequence. | Studying m10 gives the learner a hazard cue but not enough procedure-level knowledge to derive the keyed answer, so this runbook item is deeper than its assigned lesson. | Teach the four-step response in m10 or move the item to a lesson that owns water-leak runbooks; do not leave the mismatch implicit. |
| p3-025 | m15 operations / m15-q385 | content | study-then-mock | major | `m15-q385` tests the OSHA 29 CFR 1904.39 rule that a qualifying amputation is reported within 24 hours, but the m15 lesson teaches OSHA Subpart S/1910.333 electrical-work boundaries and procedure reporting, not the 1904.39 fatality/amputation reporting clock. | A learner can study the supplied m15 page and still lack the rule needed to answer the item; this is an exam concept absent from the assigned module. | Add the bounded 1904.39 reporting rule to m15 with its source, or move/withdraw the item until the teaching path owns it. |
| p3-026 | m15 operations / bank items | content | study-then-mock | keep | The m15 lesson’s organization, procedure-use-level, and Subpart S boundaries line up with `m15-q219`, `m15-q222`, and `m15-q223`; the mismatch is specifically the separate 1904.39 timeline in `m15-q385`, not the broader operations material. | The sampled module/test pass distinguishes taught concepts from an unsourced adjacent regulation instead of treating the entire module as aligned by topic name alone. | Keep module and item ids attached to this audit when future operations regulations are added. |
| p3-027 | m04 facilities / bank items | content | study-then-mock | keep | The m04 lesson covers underfloor air/cabling, concentrated loading, ceiling-plenum coordination, and the high-density slab/overhead design choice tested by `m04-q009`, `m04-q117`, `m04-q132`, and `m04-q213`. | The sample does not require a learner to know a structural or plenum decision that the lesson never introduces. | Preserve the explicit “raised floor is a design choice, not universal law” framing. |
| p3-028 | hub | 1280 | post-submit returning | keep | In a fresh browser context, I answered all 40 mock items, submitted, loaded results, then reloaded the hub; the returning hub displayed `Next up`, `Review M01`, and `Flagged weak on your last mock`, alongside the 15 module mastery cards. | A returning learner receives a concrete review destination instead of being sent back to the first-run hub with no memory of the attempt. | Preserve the weak-module handoff and make any future due-queue changes additive rather than replacing it. |
| p3-029 | hub | 1280 | post-submit returning | question | After that completed mock, `Now · 90 seconds` still said `No cards due. Take a mock or quiz, then come back for a 90-second loop`, while the separate `Next up` card correctly offered `Review M01`; the completed mock did not populate a due count in the primary session panel. | The returning learner sees useful weak-module guidance but the most prominent “now” panel still reads like the empty first-run state, leaving unclear whether mock misses are intentionally not due yet or failed to enter the queue. | Explain the distinction in the hub copy or expose the weak review in the primary 90-second panel; verify the expected post-submit due policy with a browser assertion. |
| p3-030 | hub/module mastery | 1280 | post-submit returning | keep | The post-submit hub kept module mastery separate from mock weakness: the recommendation flagged M01 from the mock, while the module cards continued to say `Not yet practiced (80%+ on a module quiz)`. | A mock result does not falsely claim that a learner has mastered a module quiz, and the two study signals remain distinguishable. | Preserve the separate mock-weakness and module-quiz mastery semantics in returning-state redesigns. |
| p3-031 | runbooks | 375/1280 | keyboard | major | The six runbook scenarios are list items with metadata paragraphs and stems, but none has a heading; the page has only the single `Runbook vignettes` h1. | A screen-reader learner cannot use heading navigation to jump between the six scenarios and must traverse the entire list to find a module or item. | Give each vignette a heading named with its scenario/module and keep the existing list semantics, then assert the six-entry outline. |
| p3-032 | reference | 375 | keyboard | keep | The reference tablist exposes two tabs with `aria-selected`/`tabindex` state, and ArrowRight moved focus from Glossary to Power & redundancy while hiding the first panel and showing the second. | A keyboard learner can switch the two reference documents without a pointer, and the active panel state is announced through the tab contract. | Preserve the tablist/tabpanel roles and arrow-key behavior. |
| p2-019 | mock | 768 | first-run | major | At Question 37 of 40 after answering Questions 1–36, the header shows only `37 / 40` and `36 of 40 answered`; it does not show a separate skipped or unanswered count. | A tired learner cannot see that four questions remain unanswered without inspecting every jump chip. | Add an explicit unanswered count that links to the remaining questions. |
| p2-020 | mock | 768 | first-run | major | The Question 37 state has no Flag or Review-later control, and the jump grid exposes only labels such as `Question 37, unanswered, current`. | A learner cannot mark three uncertain questions for a deliberate final review instead of relying on memory. | Add a per-question flag toggle and a flagged state in the jump-chip labels. |
| p2-021 | mock | 768 | first-run | major | With 36 of 40 questions answered, Submit attempt remains disabled and the only explanation is `Submit unlocks when every item has a choice`; no pre-submit state identifies the four blank questions. | The learner knows submission is blocked but not which work remains, creating avoidable end-of-exam scanning. | Show the unanswered count and identifiers, with a jump-to-first-unanswered action. |
| p2-022 | mock | 768 | first-run | major | After all 40 choices were present, clicking Submit attempt navigated directly to Results in 98.9 ms with no confirmation dialog or answer-summary step. | An accidental click can end a 60-minute attempt without a last chance to review completion state. | Add a confirmation showing answered and unanswered counts before grading. |
| p2-023 | results | 768 | post-fail | keep | A complete 40-answer submission rendered Results, the score, and the GradeExact digest in 98.9 ms without an empty intermediate state or spinner. | Immediate feedback keeps the learner from wondering whether the submission was lost. | Preserve the direct result transition; add progress feedback only if grading becomes asynchronous. |
| p2-024 | results | 768 | post-fail | keep | The observed 6/40 result says `below the practice bar of 27. Review weak modules below` and explicitly says the tool grants no credential; it uses no shaming or failure label. | A bad study day gets a neutral diagnosis and a next action instead of discouragement disguised as feedback. | Preserve the neutral score language and recovery direction. |
| p2-025 | results | 768 | post-fail | keep | Sampled incorrect cards for `m01-q201`, `m13-q212`, `m12-q220`, and `m11-q237` showed chosen/correct labels, an explanation naming why distractors fail, and a module review link. | The wrong-answer moment teaches and routes the learner to the source module rather than merely revealing a letter. | Preserve this review-card contract for every incorrect item. |
| p2-026 | mock | 768 | returning | keep | After answering Question 1 and moving to Question 2, reload restored `2 / 40` and `1 of 40 answered`; closing that tab and opening Mock in a new tab started at `1 / 40` with no draft. | Refresh recovery works while the explicit tab-scoped copy accurately sets expectations about closing the tab. | Preserve the tab-scoped persistence and its plain-language warning. |
| p2-027 | mock | 1280 | first-run | major | `m01-q201` has option lengths of 5, 8, 14, and 17 words; C and D use long negation chains such as `no human factor` that make B look like the only normally phrased answer. | A learner can use option shape and tone instead of understanding organizational causes of unavailability. | Rewrite C and D as plausible alternatives with matched specificity. |
| p2-028 | mock | 1280 | first-run | major | `m12-q220` has three visibly implausible distractors: A prescribes corrosive powder around electronics, C replaces building suppression with a portable agent, and D requires no training. | The policy-qualified B answer is identifiable by realism before the learner knows clean-agent trade-offs. | Replace A, C, and D with plausible agent-selection and training trade-offs. |
| p2-029 | mock | 1280 | first-run | major | `m06-q231` makes A, B, and C absolute claims (`zero need`, `immunity from all`, and `automatic 2N`) while D is the only bounded trade-off statement. | The lithium-ion answer can be selected by rejecting absolutes rather than reasoning about density, lifecycle, and safety. | Rewrite A–C as credible advantages or limitations with bounded language. |
| p2-030 | mock | 1280 | first-run | major | `m11-q237` keys a 37-word C option while A, B, and D are 15 words each; C also contains the only detailed physical-diversity and east-west-fabric reasoning. | Length and specificity expose the key before the learner evaluates carrier and fabric topology. | Match option scope and length, or split the two concepts into separate items. |
| p2-031 | mock | 1280 | first-run | major | `m10-q217` gives A, C, and D universal wording (`regardless of site`, `every closed loop`, and `every data centre`) while B is the only site-specific verification path. | The correct water-reuse decision is recoverable from absolute wording without applying jurisdiction and design constraints. | Replace the universal distractors with bounded site cases that require the same review. |
| p2-032 | mock | 1280 | first-run | major | `m02-q217` makes A the only option that distinguishes ERF from ERE; B invents an Availability Class rename, C asserts a universal PUE law, and D collapses two metrics into one KPI. | The metric distinction is visible through category errors before the learner checks the source or denominator. | Use realistic metric-boundary confusions as distractors instead of category errors. |
| p2-033 | mock | 1280 | first-run | major | `m03-q235` makes A and B deny any distinction while C invents a mandatory assessor signature; D is the only option compatible with self-declaration versus third-party assessment. | A learner can eliminate the alternatives without reading the TIA-942 claim carefully. | Make distractors represent realistic procurement and assessment misunderstandings. |
| p2-034 | mock | 1280 | first-run | major | `m07-q044` makes the keyed transformer/busbar/UPS set the only option containing equipment that carries substantial current; A is plastic/fibre, B is exit signage, and D is an empty tray. | The magnetic-field answer is obvious from object class rather than the learner identifying current-carrying sources near IT. | Use plausible nearby sources with different current or distance conditions. |
| p2-035 | mock | 1280 | first-run | major | `m12-q209` makes C the only option mentioning both ignition risk and permits/fire watch; A says sparks polish fibre, B waives permits for a clean-looking area, and D calls smoke harmless. | Hot-work controls can be answered by rejecting cartoon distractors instead of recognizing the actual risk boundary. | Replace A, B, and D with credible hot-work control failures. |
| p2-036 | mock | 1280 | first-run | major | `m04-q117` makes C the only option describing force through a small contact area; A, B, and D are unrelated painting, room-average power, and DNS concerns. | The concentrated-load concept is identifiable without knowing access-floor load-rating distinctions. | Make distractors describe realistic but wrong load-rating scenarios. |
| p2-037 | mock | 1280 | first-run | major | `m13-q212` makes D the only option that acknowledges a ceiling-space security bypass; A, B, and C each deny the pathway using an absolute premise. | The security-pathway answer is recoverable from the posture of the options rather than the concept. | Use plausible controls that fail in different ways while preserving one defensible answer. |
| p2-038 | mock | 1280 | first-run | major | `m05-q214` makes A the only option with a code-specific emergency-lighting source check; B, C, and D are categorical shortcuts about IT UPS, rack UPS, or generators for every luminaire. | A learner can spot the code-aware wording without separating emergency-lighting and IT ride-through requirements. | Replace the shortcuts with realistic review decisions that are incomplete but plausible. |
| p2-039 | mock | 1280 | first-run | question | Re-running the exam after the complete attempt still exposes only one Seed option, `42`; no second seed appears in the control. | The learner cannot create a meaningfully different mock from this surface, limiting repeated practice despite the control’s combobox appearance. | Expose a deliberate seed set or label the control as a fixed seed-42 fixture. |
| p2-040 | mock | 768 | timer-expired | keep | With one answer present and the stored timer moved past the deadline, the timer read `00:00` with aria-label `Timer expired at 00:00 — you may still submit`; Submit stayed disabled only because 39 answers were missing. | Expiry itself does not silently discard the draft or add a second submission block beyond the normal completion rule. | Preserve the explicit expiry label and keep the unanswered-count gate separate. |
| p2-041 | results | 768 | returning | keep | Reloading a scored 6/40 Results page preserved the score, digest, weak-module list, and item-review sections with the same headings and values. | A learner returning to review does not lose the diagnostic record after a refresh. | Preserve attempt storage and the fail-closed results reload behavior. |
| p2-042 | results | 768 | post-fail | major | At 6/40, Weak modules renders all 15 modules as bare `M01`–`M15` links with no miss count, rate, or priority order. | A learner who needs recovery guidance cannot tell which module will yield the most value first and must guess from an alphabetical list. | Add per-module misses or correctness rates and order the links by recovery priority. |
| p2-043 | mock | 1280 | first-run | major | `m05-q142` makes D the only option that identifies the egress path; A asks for detailed bench illumination, B turns an exit sign into a fire-alarm appliance, and C substitutes floor markings. | The learner can select D by rejecting three unrelated functions without knowing emergency-egress lighting. | Replace the unrelated distractors with plausible emergency-lighting misconceptions. |
| p2-044 | mock | 1280 | first-run | major | `m08-q207` makes C the only option that says blanking panels and brush strips control airflow; A calls them trim, B says to increase bypass, and D limits them to liquid cooling. | The rack-airflow answer is exposed by one normal-purpose phrase and three category errors. | Use credible leakage, recirculation, and containment alternatives with one precise distinction. |
| p2-045 | mock | 1280 | first-run | major | `m09-q227` makes C the only option naming cold plates or liquid loops; A says room air only, B replaces grounding, and D equates liquid cooling with hot-aisle containment. | Direct-to-chip cooling can be answered by vocabulary matching instead of recognizing the architecture. | Replace the category errors with plausible liquid-cooling boundary mistakes. |
| p2-046 | mock | 1280 | first-run | major | `m11-q110` makes C the only option describing longer reach and bandwidth headroom; A, B, and D use absolute goals such as always-short jumpers, deliberate dispersion, and no connectors. | The fiber-selection answer is visible from the only technically normal option. | Give each distractor a realistic reach, mode, or installation trade-off. |
| p2-047 | mock | 1280 | first-run | major | `m06-q107` makes B the only operational sequence; A removes UPS ride-through, C turns the fire system into a power path, and D leaves only lighting. | A learner can answer the outage sequence by rejecting impossible system roles rather than reasoning about generator transfer. | Replace the cartoons with plausible sequencing errors around UPS, ATS, and generator stabilization. |
| p2-048 | mock | 1280 | first-run | major | `m09-q151` makes D the only option about preventing containment leakage and mixing; A, B, and C answer with separation, battery autonomy, and CRAH redundancy. | The containment question is solvable by topic mismatch before the learner considers why doors and blanks matter. | Keep all distractors within containment or thermal-control decisions. |
| p2-049 | mock | 1280 | first-run | major | `bank-m14-q106` makes B the only option that connects inventory, capacity, and workflow to telemetry; A, C, and D are specialized release, switchgear, and protection-relaying functions. | DCIM’s differentiator is exposed by noun-category matching rather than comparison with a credible EMS boundary. | Use realistic EMS/DCIM overlap cases as distractors. |
| p2-050 | mock | 1280 | first-run | major | `m04-q200` makes C the only option that distinguishes area, point, and wheeled-equipment forces; A denies load distribution, B calls the issue aesthetic, and D reduces it to cable weight. | Access-floor load-rating knowledge is unnecessary when three options reject the concept outright. | Build distractors from realistic uniform, point, and rolling-load misunderstandings. |
| p2-051 | mock | 1280 | first-run | major | `m07-q045` makes B the only ordinary mitigation statement; A proposes RF-absorbing paint, C removes bonding, and D changes distribution phase. | Distance and layout can be selected from the safety posture of the options instead of magnetic-field reasoning. | Replace unsafe or irrelevant proposals with credible mitigation trade-offs. |
| p2-052 | mock | 1280 | first-run | major | `bank-m13-q103` makes C the only option that admits weak layers at roofs, yards, or controls; A and B claim universal perfection while D is an unrelated plant procedure. | The security-theatre concept is visible from one bounded statement versus three absolutes or a topic mismatch. | Use plausible security-layer gaps that require applying the theatre concept. |
| p2-053 | mock | 1280 | first-run | major | `bank-m14-q124` makes D the only option naming an actionable next step; A, B, and C are visibly absurd operator-log practices. | The alerting principle can be answered by tone and actionability without understanding runbook design. | Replace the absurd distractors with realistic alert-routing and runbook failures. |
| p2-054 | quiz | 1280 | first-run | major | Module-06 quiz item `m06-q066` makes C the only normal load-bank statement; A permanently disables emissions controls, B removes fuel contracts, and D replaces weekly battery checks. | The learner can select the keyed generator answer by rejecting three impossible consequences. | Replace A, B, and D with plausible load-bank limitations or maintenance trade-offs. |
| p2-055 | quiz | 1280 | first-run | major | Module-06 quiz item `m06-q075` makes B the only bounded comparison; A says one design is universally better, C removes all labeling, and D removes all breakers. | The infrastructure-choice answer is visible from absolute wording before the learner weighs agility, density, or failure domains. | Use credible busway-versus-cable trade-offs for every distractor. |
| p2-056 | quiz | 1280 | first-run | major | Module-06 quiz item `m06-q083` makes B the only environmental qualification; A reduces enclosure protection to runtime, C reduces the concept to IP version, and D substitutes fuel octane. | The learner can answer ingress protection by category matching rather than understanding the enclosure requirement. | Use realistic enclosure, cooling, and maintenance conditions as distractors. |
| p2-057 | quiz | 1280 | first-run | major | Module-06 quiz item `m06-q090` makes C the only bounded health-monitoring benefit; A, B, and D are impossible consequences of reliability or battery testing. | Predictive replacement is identifiable from option sanity rather than maintenance reasoning. | Give all four options credible predictive-maintenance outcomes. |
| p2-058 | quiz | 1280 | first-run | major | Module-06 quiz item `m06-q094` makes C the only option containing a valid electrical relationship; A, B, and D use absolute or unrelated quantities. | The power-factor answer is a formula-recognition cue instead of an applied distinction between kVA, kW, and PF. | Replace the false absolutes with plausible measurement and sizing errors. |
| p2-059 | quiz | 1280 | first-run | major | Module-06 quiz item `m06-q097` makes A the only option with both scanning and trend analysis; B, C, and D each discard the historical or loaded condition. | The thermal-inspection practice can be selected from the only option that preserves a usable evidence trail. | Use realistic inspection cadences and evidence-quality trade-offs. |
| p2-060 | quiz | 1280 | first-run | major | Module-06 quiz item `m06-q108` makes D the only credible light-load exercise statement; A, B, and C are absolute fuel, loading, or exhaust claims. | Generator operating practice is answerable by rejecting physically implausible alternatives. | Replace the absolutes with credible light-load risks and mitigation choices. |
| p2-061 | quiz | 1280 | first-run | major | Module-06 quiz item `m06-q205` makes B the only option naming a real UPS consequence; A, C, and D are unrelated fuel, harmonic, or bonding claims. | The learner can identify overload and lost redundancy without reasoning about spare capacity. | Keep every distractor within plausible UPS-capacity and redundancy outcomes. |
| p2-062 | quiz | 1280 | first-run | major | Module-06 quiz item `m06-q237` makes B the only option that describes a transformer’s distribution role; A, C, and D assign it flywheel, STS, or containment functions. | Equipment-role knowledge is replaced by obvious category elimination. | Use nearby plant equipment with realistic but incorrect voltage-conversion roles. |
| p2-063 | quiz | 1280 | first-run | major | Module-06 quiz item `m06-q243` makes D the only technically ordinary STS statement; A, B, and C assign it generator-start, generator-feed, or battery-replacement behavior. | The sensitive-load transfer question is solvable from equipment-role absurdity. | Use credible STS, ATS, and generator-sequencing confusions. |
| p2-064 | quiz | 1280 | first-run | major | Module-06 quiz item `m06-q255` keys a 37-word B option while A, C, and D are 6–9-word topology labels; B is the only option that explains why two utility names do not establish 2N. | Option length and explanatory specificity expose the key before the learner inspects the SPOF diagram. | Match option scope and require the same topology distinction in every choice. |
| p2-065 | quiz | 1280 | first-run | major | Module-06 quiz item `m06-q260` keys a long layer-specific B explanation while A, C, and D are plaque or automatic-rating cartoons. | The tier-capacity answer is visible from length and realism rather than checking N at each layer. | Rewrite distractors as plausible layer-boundary and spare-capacity mistakes. |
| p2-066 | quiz | 768 | post-fail | major | A 1/12 Module-06 result says `Missed items sent to Drill` but does not state the missed count on the score block. | The learner cannot quantify the recovery queue until opening another surface. | Show `11 missed` beside the score and keep the Open Drill action. |
| p2-067 | quiz | 768 | post-fail | keep | The 1/12 quiz result included the WASM digest, every chosen/correct pair, a distractor-aware explanation, Retake module, and Open Drill in the same result state. | A low score still gives an immediate learning path rather than a dead-end grade. | Preserve the complete quiz-review and recovery action set. |
| p2-068 | quiz | 768 | first-run | major | After all 12 answers were present, Grade quiz entered the result state directly without a confirmation or answer summary. | A learner can end a module quiz accidentally without reviewing the last answer set. | Add a brief confirmation showing the answered count before grading. |
| p2-069 | quiz | 768 | post-fail | keep | The failed quiz state keeps Retake module and Open Drill visible beside the review, so the learner can either retry or practice misses without returning through Hub. | Recovery actions are available at the moment a poor score is revealed. | Preserve both actions and their placement near the result. |
| p2-070 | quiz | 1280 | first-run | major | Module-06 pre-quiz copy tells the learner `approved pool 140 of 146 in bank` while the actual quiz contains 12 items. | Internal approval and bank totals distract from the learner’s real scope and make the sample size harder to understand. | Replace the authoring-pool detail with a learner-facing `12 questions in this quiz` statement. |
| p2-071 | quiz | 1280 | first-run | minor | The pre-quiz instruction says `Sample items from one bank module (BankItem.module)`, exposing an internal type name in learner copy. | Schema jargon makes a study instruction feel like a developer diagnostic and undermines the otherwise plain-language surface. | Replace `BankItem.module` with `the selected module’s question bank`. |
