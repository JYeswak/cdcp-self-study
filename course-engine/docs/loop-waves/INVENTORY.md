# Loop wiring inventory — course-engine (wave 1 input)

Generated by `loop-wave` against the live tree. Do not treat this as a tick.

| Probe | Path | Wiring |
|---|---|---|
| emit-tick computes product_moved | `crates/cdcp_bank/src/tick_emitter.rs` | Wired |
| forbidden phrases rejected | same | Wired |
| check.sh choke | `scripts/check.sh` | Contradicted (`not a verdict-producing gate`) |
| pre-commit | `hooks/pre-commit` | Unwired (`substrate-guard --staged` only) |
| gate wrapper | `crates/cdcp_gate/src/gates/emit_tick.rs` | Wired as writer, not as hook |
| Charter Rule Zero | `.flywheel/CHARTER.md` | Wired as doctrine |
| tick ledger | `.flywheel/tick-ledger.jsonl` | Wired as artifact |
| franken-harvest | `~/.claude/references/franken-harvest.md` | Wired as standing ledger |

Finding: **comment-only emit-tick choke** at pre-commit/`check.sh`.
Pre-push `tick-guard` is the live delivery choke (push of `18ec55ec` refused
until an expiring exemption).
