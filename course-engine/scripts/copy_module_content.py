#!/usr/bin/env python3
"""Retired alias — use `cdcp build-learn` (bd-substrate-rust-migration-jhd.28)."""
import sys

print(
    "FAIL: scripts/build_learn.py is retired; use `cdcp build-learn`",
    file=sys.stderr,
)
sys.exit(1)
