[package]
name = "cdcp_learn"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "LEARN compilers: units_index.json and glossary.json (learner-visible product, not a gate)"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
toml = { workspace = true }

[dev-dependencies]
tempfile = "3"
