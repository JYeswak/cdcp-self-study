[package]
name = "cdcp_gate"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "cdcp gate dispatcher: Rust-native repo gates (substrate floor + ported gates)"
publish = false
build = "build.rs"

[[bin]]
name = "cdcp_gate"
path = "src/main.rs"

[dependencies]
serde = { workspace = true }
toml = { workspace = true }

[dev-dependencies]
tempfile = "3"
cdcp_learn = { workspace = true }
