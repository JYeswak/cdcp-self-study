# WORK QUEUE — 2026-08-20

**Self-serve.** When you finish an item, do not wait for a dispatch. Claim the next
unclaimed item in this file, append your pane number to its `claimed:` line, commit that
one-line change, and start. If two panes race, the lower pane number yields and takes the
next item.

## The thesis for today

Two P0s say the same thing: **throughput exceeds verification.** CI has never completed a
run, and the tick ledger stopped 34 hours and 146 commits ago. So every "green" cited
yesterday is local, unrecorded, and unaudited. That does not mean the work was bad — it
means the loop cannot see it, and neither can anyone else.

Today, in order:

1. **Make the loop visible.** Ledger gets an edge to sit on; CI completes one run.
2. **Make the product reachable.** A stranger installs `cdcp` and sits 40 questions.
3. **Keep the bank honest.** Residual construction cues to chance. This never runs out and
   is the default filler when a lane blocks.

The bank is the product. Installability is how the product reaches anyone. The loop
machinery is the instrument — it is not the goal, and a day spent on instruments scores
zero under Rule Zero.

## Standing constraints

- **No GitHub Actions.** All CI is local. (~$2k already spent on the other path.)
- **Do not raise `ceiling_lines` in `registries/gate_shrink.toml`** without a CI GREEN
  receipt on the same SHA.
- **Weakening a gate is escalation-only. Strengthening is fully autonomous.** If your fix
  needs a threshold loosened, stop and report — do not loosen it.
- **Scope `git add` to exact paths, never `-A`.** Several panes share one worktree.
- A single RED on a file another pane owns is **provisional** until re-measured once. A
  torn read can only produce a false RED, never a false GREEN.
- Report every coupled property you touched, moved or not. A property you did not measure
  is a property you are claiming by silence.

---

## LANE 1 — Loop visibility (P0)

### Q1 · Wire the tick ledger to an edge that blocks · claimed: controller · **DONE** (95038d54)
`bd-engine-not-gate-ar39` family. Build the close-gate: a `br close` claiming product
movement must carry a tick receipt naming commit + evidence. Not a clock pulse — a gate on
the edge where work is declared done. Prove it DENIES a real attempt; it scores zero until
it does.

### Q2 · Reconcile the 34h ledger gap · claimed: controller · **DONE** (T12)
One honest row naming the window, the 146 commits, and why nothing was written. Do not
fabricate 146 ticks. Then let `watchdog-escalate.sh` resolve `URGENT_JOSH.md` on its own
next healthy observation.

## LANE 2 — CI completes one run (P0)

### Q3 · CI trigger is wrong, not the runner · claimed:
`bd-installability-sm4g.22` + `bd-engine-not-gate-ar39.15`. Gate re-runs on
consumed-input bytes, not on HEAD movement. Pin one SHA per run; never straddle two
revisions. Name the 18 divergent gate_shrink lines individually.

### Q4 · Flip ported gates to cdcp_gate exit codes · claimed: pane 3
`bd-2m9`. One commit, after the oracles die. Blocked until Q3 lands a completed run —
otherwise you cannot tell a real red from an unfinished chain.

### Q5 · gate_shrink ratchet pushed assertions out of the scan set · claimed:
`bd-kax4` (P1). The ratchet optimised for line count and drove assertion logic outside the
anti-vacuous scan region — so the scan is measuring a shrinking subset of itself. Prove
both legs: a planted bad input inside the scan set still goes RED, and the assertions that
moved are back under scan.

## LANE 3 — Substrate (P1/P2)

### Q6 · Anti-vacuous floor is stale after retirements · claimed: controller · **DONE** (c0186fb5) — it was self-contradictory, not stale; floor now derived from the allowlist, bead closed
`bd-substrate-floor-after-retirements-mfz9`. Floor wants 42 non-Rust files; tree has 40.
All five departures are accounted for and **three are Substrate Law wins** (Python → Rust).
A count-based floor must eventually fail when the project succeeds — that is a gate design
defect, not a project defect.
**Build the structural replacement and prove both legs. Do NOT adopt it.** The replacement
must go RED if the scan set is emptied, RED if paths are excluded, RED if the plant is
removed, GREEN on today's healthy 40-file tree. Bring the receipt; adoption is one line of
approval, not your call.

### Q7 · Continue Python → Rust gate migration · claimed:
`bd-substrate-python-gates-viu` / `bd-substrate-rust-migration-jhd`. 17 Python gates remain
on the load-bearing path. **Coordinate with Q6** — each port lowers the count-based floor
further, so land the structural floor first or the chain reds on your own success.

## LANE 4 — Bank quality (never runs out; default filler)

### Q8 · Stem-overlap cue 38.6% → chance · claimed: pane 3 · **DONE** (27.2% vs 25% floor, T13, bead closed)
`bd-9qbo`. Raise distractors into the stem's vocabulary; do not strip keys. Full coupled
property table required.

### Q9 · Consolidated construction-fault receipt · claimed: · **DONE** (7971ab0e; rechecked 2026-08-20)
One run of the full property suite on predeclared seeds 0..99, all cues in one table:
length-rank, key skew (bank + 15 per-module), hedged-option, stem-overlap, all-of-the-above,
grammatical agreement. This is the bank's single citable quality artifact. Predeclare the
seeds; do not pick new ones after seeing a result.

### Q10 · Curriculum truth · claimed: · **DONE** (bead closed T18; no new content work)
`bd-curriculum-truth-ebrr` (P2 epic). 2026 interview-accurate teaching corpus. Decompose
before working — it is an epic, not a task.

## LANE 5 — Installability → the first external signal (P0 epic)

### Q11a · The three stranger-test P0s · claimed: · **DONE** (beads .26/.27/.28 already closed)
`bd-installability-sm4g.26` undocumented cargo prerequisite · `.27` rustup with no default
toolchain (done, 6b21e0d8) · `.28` **doctor falls back to the source checkout**. `.28`
outranks the others: a doctor that silently walks up into a source tree reports healthy on a
machine where the learner surface is broken, which converts a real break into a confident
pass. Doctor must name which root it resolved and by which precedence step, every run.

### Q11 · Release engineering · claimed:
`bd-installability-sm4g.8`. Workflow, triples, artifacts. Local only — no GitHub Actions.

### Q12 · The stranger test · claimed: pane 3 · **DONE** — install from an empty dir in 66.78s, 40 questions answered through WASM, scored 6/40; negative leg exit 4. Filed .26/.27/.28. NOTE: it passed only because the tester had a Rust toolchain, which is the developer case, not the stranger case.
From a clean directory with nothing preinstalled: one command → `cdcp` resolves its own
bundle → serves the learner app → a human answers 40 questions → gets a score.
**Whatever breaks is the bead.** File each break separately. This is the precondition for
any loop-3 signal, and we currently have none.

### Q13 · Public README rigor · claimed:
`bd-readme-public-rigor-8y0r` (in progress). Finish and close with cited evidence.

## LANE 6 — Loop 3 (the only thing that makes "shipped" honest)

### Q14 · Get one real human through 40 questions · **HUMAN-ONLY — NO AGENT MAY CLAIM THIS**

**Do not claim it, do not attempt it, do not mark it done.** Its entire value is that it
comes from outside the system that built the bank. An agent sitting the exam — however
carefully, however isolated the environment — produces an internal proxy wearing the label of
an external signal. That is *worse* than no signal, because it lets the charter's
`shipped_means` be marked satisfied by the very machinery it is meant to audit. **A loud gap
beats a quiet fake.**

A person who did not build this, sitting a mock exam, their score, and what confused them.
The bank has never been sat by a real candidate. Every quality number we hold — cues at
chance, keys at 25.2/26.1/24.6/24.1, 534/318/105 cognitive levels — is an internal proxy for
this one signal, and none of them substitute for it.

**What an agent MAY do here:** build the instrument that makes it trivial for a real person
to sit the mock and for us to capture their score and confusion points. Build it; do not sit
the exam with it. Done — `docs/loop3/EXTERNAL-HUMAN-MOCK.md`, which correctly states that an
agent run, a maintainer self-run, `cdcp demo`, and a synthetic answer file do not satisfy Q14.

Blocked on a human being asked. Unblocks the charter's `shipped_means`.

> **Why this marker exists.** A self-serve queue hands out whatever is in the list. On
> 2026-08-20 a pane claimed this item and would have completed it as an agent run — not from
> bad faith, but because the queue said it was work and nothing said it could not be. Any
> item whose value depends on coming from outside the system needs this marker, or autonomy
> quietly converts an honest blocker into a fabricated completion.

---

## Denominator note

Predeclare seeds, scan sets, and item populations **before** measuring. When a rate moves
because the denominator moved — as the hedged-option population did when the cartoon
distractor work changed option sets — say so explicitly rather than reporting the delta as
an improvement.

---

# REFILL — added 18:35Z

The queue went functionally dry: pane 3 held six claims it can only work one at a time, and
the two unclaimed items were gate-chain work fenced off from it. Keeping this stocked is the
controller's job, not the panes'.

**Release what you are not actively working.** A claim is "I am on this now", not a
reservation. Hold one item at a time.

### Q15 · Watchdog false-alarms during legitimate long work · claimed:
`bd-lnm3` (P1). `.flywheel/watchdog.sh` fires STALL on 45 minutes of elapsed clock. Beads
here take 1–2 hours, so it is structurally unable to be quiet — which is how 111 STALL rows
and an `URGENT_JOSH.md` accumulated unread. **Time-since-tick is the wrong axis**; it
advances during productive work. The condition that actually indicates a bypassed loop is a
bead closed without a receipt, which `cdcp_gate tick-reconcile` already detects on a blocking
edge. Known-good leg: two agents working two hours with no closes produces NO alert.
Known-bad: one unreceipted close still alerts. Not gate-chain — does not collide with pane 2.

### Q16 · The inverse hedge cue: avoid-hedge is 29.3% · claimed:
`bd-d97q` is still open. Pane 3 measured lone-hedge at 21.6% (good) but **avoid-hedge at
29.3%** — a candidate who systematically *avoids* the qualified option beats chance. Fixing
one direction pushed the cue into the other. Both must sit near 25%. Predeclare seeds 0..99,
report both numbers, and re-measure the full coupled property set.

### Q17 · Cognitive level: does the bank actually challenge? · claimed:
No gate measures this and it is the single biggest unmeasured product property. Classify all
957 items by cognitive demand — recall a fact / apply a rule to a situation / analyse a
trade-off — and report the distribution per module. A bank that is 90% recall does not test
judgement no matter how clean its option statistics are, and this project already fought that
fight once (`bd-0cjy`: recall-only 52 → 0). Deliver the measured distribution FIRST and the
proposed target second; do not start rewriting items before the number exists. State plainly
what the classifier cannot decide — this is a judgement property and a mechanical proxy for
it will be wrong at the margins.

### Q18 · Blueprint coverage against the real exam weighting · claimed:
Do the 15 modules appear in the assembled 40-item mock in proportion to the actual CDCP
blueprint, or in proportion to how many items we happen to have written? If a module is
over-represented because authoring was easy there, the mock is not a mock. Measure assembled
module frequency over predeclared seeds 0..99 against the published weighting; where the
weighting cannot be sourced publicly, say so and do not invent one.

### Q19 · CDFOS daily-ops content · claimed:
`bd-epi-ecosystem-ms4j.2` (in progress, P2). Content work, no gate-chain collision.

### Q20 · Conversion candidates: the path out of 56% recall · claimed:
The inventory measured **534 recall / 318 apply / 105 analyse** — 56% recall, 11% analyse.
That is the honest answer to "does this bank challenge anyone," and it is a larger quality gap
than any option-statistics cue, because a candidate who memorises 534 facts passes without
ever exercising judgement.

**Do not rewrite items. Produce the targetable list.** For the 534 recall items, identify which
are convertible to apply-level *without inventing content the corpus does not support*. Per
module: convertible / not / uncertain, at least 10 actual proposed conversions so the judgement
is inspectable, and a one-line reason for each rejection.

Should-fail: include one item you first judged convertible and then rejected, and say what
changed your mind. A pass where everything is convertible is a pass that stopped thinking.
Green-does-not-prove: a conversion list proves the items *could* be harder. It does not prove
the conversions are correct, sourced, or pedagogically better — a human reviews that before a
single item changes.

## Claim hygiene

**A claim means "I am on this now", not a reservation. Hold one at a time.** Twice today a pane
went idle with nowhere to go because the other pane held most of the queue. Releasing a claim
you are not working costs nothing; holding it stalls the other pane.

## Note: `br ready` pollution

`br ready` returns beads from unrelated projects (`bd-3m3v` Admin PhaseControls, `bd-2abb` Swarm
v10 validator). Both panes correctly refused them. Do not chase these; the queue file is the
authority for this repo's work.

---

# STANDARDS ADOPTION — added 03:50Z, filed against `beads-north-star`

`fh` (franken-harvest) is now built locally at `~/.local/bin/fh`. `fh search` / `fh suggest`
work **without** the Studio mirror — the ledger travels with the binary. Its nine measured
standards describe defects **seven of which this repo hit on 2026-08-20**. Each bead names the
local measured instance, not just the upstream one, and each requires an **in-tree known-bad
specimen** — never an apply-patch harness, because `git apply` silently no-ops on an index-hash
miss and a fires-on-known-bad that cannot fire is a fooled certificate.

### Q22 · Rendered-output contracts · claimed: pane 3
`bd-std-rendered-output-5moj` (P1). **Highest product value of the seven.** Every quality number
we hold is internal. Nothing asserts the string a learner actually reads through WASM. Perfect
keys plus a letter-shifting render grades every candidate wrong while all 957 items measure
clean. Enumerate presentation sites, assert complete rendered strings through the learner entry
point, report correct *and* defective sites against a predeclared denominator.

### Q23 · Hermetic shared-tree tests · claimed:
`bd-std-hermetic-tree-mo9w` (P1). Per-lane target dirs; fingerprint HEAD and product inputs
before and after; return DRIFT when inputs move mid-run. Our "a lone RED is provisional" rule
covers false **REDs** only and offers nothing against the false **GREEN** upstream measured — a
stale test binary passing 2/2 against the wrong bytes. **Blocks Q24.**

### Q24 · Causal known-bads · claimed: — BLOCKED BY Q23
`bd-std-causal-known-bads-2nt8` (P1). Every fixture must prove ONE named detector: bypass it and
the fixture must fail; intact and it must pass. `tick-reconcile`, `pack_freshness` and the
derived floor all assert violation counts, not named branches.

### Q25 · Canonical workspace identity · claimed:
`bd-std-workspace-identity-pu43` (P2). Cheapest falsifier — take it when the queue is thin.
`pwd -P` preflight. Two specimens: a different checkout must refuse; a symlink must normalize to
one identity, not refuse.

### Q26 · Value-bearing gates · claimed:
`bd-std-value-bearing-gates-1ij7` (P2). Trace every gate's input backward to a real product
artifact and its invocation forward to a real delivery surface. **Non-vacuity: this closes only
if at least one gate is actually retired or justified in writing.** Do not build a validator to
police it — that would itself fail the test.

### Q27 · Central verification surface · claimed:
`bd-std-central-verification-uevw` (P2). One matrix owns runnable commands; other docs point.
Report copies removed — zero removed means close as not-applicable, not as done.

### Q28 · Artifact identity and drift · claimed:
`bd-std-artifact-identity-byda` (P2). Full object ids, digests recomputed from staged bytes.
Two specimens: abbreviated-vs-full of the same commit is NOT drift; a one-character digest
mismatch IS.
